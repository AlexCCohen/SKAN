int main(){
    int x=10;
    print_str("x is originally declared to be 10");
    print_str("after passing through the if statment x should be set to 1, otherwise it will be set to 0");
    if (1 < x) x = 1;
    else x = 0;
    print_str("x is: ");
    print(x);
    return 0;
}